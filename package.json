{
  "name": "carrier-integration-service",
  "version": "1.0.0",
  "description": "Shipping carrier integration service wrapping the UPS Rating API with extensible architecture for additional carriers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit",
    "demo": "tsx src/demo.ts"
  },
  "keywords": [
    "shipping",
    "carrier",
    "ups",
    "rating",
    "integration"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "nock": "^14.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "zod": "^3.24.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
